{% extends 'base.html.twig' %}

{% block title 'Accueil' %}

{% block body %}

    <div class="hero-img">
        <h1>Phrase d'accroche</h1>
        <img src="{{ asset('images/tricks/trick1.png') }}" alt="Bannière"/>
    </div>
    Je suis {{ name.firstname }} {{ name.lastname }}, et j'ai {{ age }} ans.

    {% for trick in tricks %}
        <div>{{trick}}</div>
    {% endfor %}


{% endblock %}